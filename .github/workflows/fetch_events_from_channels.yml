name: Fetch Events From Channels

on:
  workflow_dispatch:
  repository_dispatch:
    types: [fetch-events-from-channels]

permissions:
  contents: write

jobs:
  fetch-events-from-channels:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Checkout Data Branch
        uses: actions/checkout@v7
        with:
          ref: data
          path: data
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Run Fetch Events From Channels Script
        env:
          THESPORTSDB_BASE_URL: ${{ secrets.THESPORTSDB_BASE_URL }}
          THESPORTSDB_API_KEY: ${{ secrets.THESPORTSDB_API_KEY }}
          MAX_RUNTIME_SECONDS: "240"
          FORCE_REFRESH: ${{ github.event_name == 'workflow_dispatch' }}
          PYTHONPATH: ${{ github.workspace }}
        run: python scripts/fetch_events_from_channels.py

      - name: Commit and Push to Data Branch
        run: |
          cd data
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add events_v2.json channel_sync_state.json queues/
          if ! git diff --staged --quiet; then
            TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
            git commit --amend -m "Automated data: Updated events_v2.json at $TIMESTAMP"
            git push --force origin data
          else
            echo "No changes detected. Skipping commit."
          fi
