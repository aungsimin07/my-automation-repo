name: Add Playlist

on:
  workflow_dispatch:
    inputs:
      playlistId:
        description: "Short unique id for this playlist (e.g. iptv-org-sports)"
        required: true
        type: string
      playlistUrl:
        description: "M3U playlist URL"
        required: true
        type: string
      tvgIds:
        description: "Optional comma-separated tvg-id(s) to track from this playlist"
        required: false
        type: string

permissions:
  contents: write

jobs:
  add-playlist:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Checkout Data Branch
        uses: actions/checkout@v7
        with:
          ref: data
          path: data
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.10'

      - name: Run Add Playlist Script
        env:
          PLAYLIST_ID: ${{ github.event.inputs.playlistId }}
          PLAYLIST_URL: ${{ github.event.inputs.playlistUrl }}
          TVG_IDS: ${{ github.event.inputs.tvgIds }}
          PYTHONPATH: ${{ github.workspace }}
        run: python playlists/add_playlist.py

      - name: Commit and Push to Data Branch
        run: |
          cd data
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add playlists.json
          if ! git diff --staged --quiet; then
            TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
            git commit --amend -m "Automated data: Updated playlists.json at $TIMESTAMP"
            git push --force origin data
          else
            echo "No playlists.json changes detected. Skipping commit."
          fi
